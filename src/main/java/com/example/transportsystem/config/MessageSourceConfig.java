package com.example.transportsystem.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MessageSourceConfig {
    // Intentionally empty.
    // Spring Boot auto-configures MessageSource from spring.messages.* properties
    // (see application.properties: spring.messages.basename, spring.messages.encoding).
}
