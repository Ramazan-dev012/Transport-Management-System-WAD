# Local/dev profile: pick a free port automatically to avoid "Port already in use"
#server.port=0

# Use a fixed, predictable port for local development so the browser can open a known URL
server.port=8080
